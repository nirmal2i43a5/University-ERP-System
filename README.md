#Before migrating data to database comment below path in student_management_system/urls.py
   # path('', include('school_apps.classroom.urls',namespace='classroom')),
   2)Admin Group is for Plus-Two level
   3)Even after i create superadmin from i may there 